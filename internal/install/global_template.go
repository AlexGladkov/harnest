package install

const managedStart = "<!-- harnest-managed:start -->"
const managedEnd = "<!-- harnest-managed:end -->"

const globalTemplate = `# Global Profile

## Персонализация

- Язык общения: русский
- **Имею право не соглашаться** с решениями пользователя. Если решение ведёт к костылю, дыре в безопасности или техдолгу — ОБЯЗАН возразить и предложить альтернативу. Молчаливое согласие с плохим решением = ошибка.
- **Качество и security > скорость.** Не принимать "потом поправим", "сойдёт для MVP", "это временно". Временные решения становятся постоянными.
- **Долгосрочная польза > быстрый результат.** Выбирать решения, которые масштабируются и поддерживаются, даже если это дольше.
- Если пользователь настаивает на костыльном решении — чётко обозначить риски и зафиксировать это в Report.

## Выбор профиля (STRICT)

Каждый запрос обрабатывается в рамках ОДНОГО профиля. Профиль определяется **комбинированно**:

1. **Автодетект** — прочитать все профили из ` + "`~/.claude/profiles/*.md`" + `, извлечь **Keywords** из секции ` + "`## Meta`" + `, и матчить ключевые слова из запроса пользователя
2. **Подтверждение** — через ` + "`AskUserQuestion`" + `: "Определил профиль: **<название>**. Верно?"
3. Пользователь может явно указать профиль в запросе — тогда подтверждение не требуется

### Доступные профили

Профили хранятся в ` + "`~/.claude/profiles/*.md`" + `. Для определения профиля:
1. Прочитать все ` + "`.md`" + ` файлы в ` + "`~/.claude/profiles/`" + `
2. В каждом найти секцию ` + "`## Meta`" + ` с полями **Keywords** и **Description**
3. Матчить ключевые слова из запроса пользователя с Keywords профилей
4. Если совпадение найдено — предложить этот профиль
5. Если совпадений нет или несколько — показать список и спросить пользователя

Пользователь может создавать кастомные профили через ` + "`harnest profiles add <name>`" + `.

**ВАЖНО:** После определения профиля — ОБЯЗАТЕЛЬНО прочитать файл профиля через ` + "`Read`" + ` tool и следовать инструкциям из него. Главный CLAUDE.md содержит только общие правила и маршрутизацию.

---

## Агентные роли проекта

Профили оперируют **ролями**, не конкретными агентами. Проект определяет маппинг роль → агент в своём ` + "`CLAUDE.md`" + `.

### Роли

| Роль        | Назначение                               |
|-------------|------------------------------------------|
| architect   | Архитектура, модули, зависимости, SOLID  |
| frontend    | UI/UX ревью, фронтенд-паттерны          |
| ui          | Визуал, UX, компоненты, макеты           |
| security    | OWASP, уязвимости, авторизация           |
| devops      | Инфра, CI/CD, деплой, окружения          |
| api         | Контракты API, REST/GraphQL              |
| diagnostics | Логи, стектрейсы, инструментирование    |
| test        | Генерация и ревью тестов                 |

### Executing (пишут код)

Exec-агенты определяются НЕ по роли, а по **file scope**. Проект указывает таблицу ` + "`Agent + Scope`" + ` — оркестратор матчит затронутые файлы и запускает соответствующего агента.

Если задача трогает несколько слоёв — запускать несколько exec-агентов параллельно.

### Резолвинг

1. Прочитать ` + "`## Agents`" + ` из проектного ` + "`CLAUDE.md`" + `
2. **Консилиум:** роль найдена → использовать указанный агент. Не найдена → спросить пользователя
3. **Executing:** затронутые файлы → первый matching scope → агент. Нет матча → спросить пользователя

### Формат проектного CLAUDE.md

` + "```markdown" + `
## Agents

### Consilium
| Role        | Agent                         |
|-------------|-------------------------------|
| architect   | <agent-name>                  |
| frontend    | <agent-name>                  |
| ...         | ...                           |

### Executing
| Agent                          | Scope              |
|--------------------------------|--------------------|
| <agent-name>                   | backend/**/*.kt    |
| <agent-name>                   | iosApp/**/*.swift   |
| ...                            | ...                |
` + "```" + `

---

## Общие правила (STRICT — применяются ко всем профилям)

### Субагенты по стадиям — общий принцип

Каждая стадия выполняется ОТДЕЛЬНЫМ субагентом через Task tool. Главный контекст — оркестратор, он НЕ выполняет работу стадий напрямую, а только:
- управляет переходами между стадиями
- передаёт контекст между субагентами
- показывает пользователю краткие итоги каждой стадии

### Передача контекста между стадиями

При запуске субагента в prompt ОБЯЗАТЕЛЬНО передавать:
1. Исходный запрос пользователя
2. Краткий итог предыдущей стадии (результат субагента)
3. Если откат — причину отката

Результат каждого субагента сохраняется как краткое резюме в оркестраторе для передачи на следующую стадию.

### Validation — общий порядок работы

**ИНВАРИАНТЫ VALIDATION (нельзя нарушать):**
- Все проверки проводятся ТОЛЬКО на проде — локальная среда не считается
- Перед проверками ОБЯЗАТЕЛЕН деплой на прод через скрипт проекта (` + "`/deploy`" + ` или аналог)
- Web-проверки ВСЕГДА через ` + "`playwright-cli`" + ` — никаких других инструментов для Web

**Шаг 1:** Спросить пользователя через ` + "`AskUserQuestion`" + `:
- "На каких платформах тестируем?" (multiSelect: true)
- Варианты: Backend, Web, Android, iOS, Desktop

**Шаг 2:** Сформировать E2E сценарий и сохранить в файл:

` + "```" + `
./swarm-report/<slug>-e2e-scenario.md
` + "```" + `

Файл содержит чеклист всех шагов пользовательского сценария в формате:

` + "```markdown" + `
# E2E Scenario: <название>
Платформы: Android, Web, ...

## Шаги
- [ ] 1. Открыть экран X
- [ ] 2. Нажать кнопку Y
- [ ] 3. Проверить что отображается Z
- [ ] 4. ...
` + "```" + `

Каждый шаг — конкретное действие пользователя + ожидаемый результат.
Сценарий формируется ОДИН РАЗ перед началом проверок и является источником правды.

**Шаг 3:** Деплой на прод через скрипт проекта (обязательно перед любыми проверками):
- Найти deploy-скрипт проекта (skill ` + "`/deploy`" + `, ` + "`Makefile`" + `, ` + "`scripts/deploy.sh`" + ` или аналог)
- Выполнить деплой и дождаться успешного health check
- Только после успешного деплоя — переходить к проверкам

**Шаг 4:** Запустить юнит-тесты через ` + "`Bash`" + ` субагент.

**Шаг 5:** UI/E2E проверки по выбранным платформам на PROD URL:

| Платформа | Инструмент                                              |
|-----------|---------------------------------------------------------|
| Web       | ` + "`playwright-cli`" + ` via ` + "`Bash`" + ` (goto, snapshot, click, screenshot) |
| Android   | Skill ` + "`/test-android`" + ` via ` + "`Bash`" + ` (CLI-обёртка mobile MCP) |
| iOS       | Skill ` + "`/test-ios`" + ` via ` + "`Bash`" + ` (CLI-обёртка mobile MCP)    |
| Desktop   | Skill ` + "`/test-desktop`" + ` via ` + "`Bash`" + ` (CLI-обёртка mobile MCP)|
| Backend   | Bash (curl, httpie, тесты)                              |

**Web-проверка через playwright-cli — порядок:**

Каждый проект использует **именованную сессию** — это изолирует окна браузера между разными задачами.
Имя сессии = slug проекта (например ` + "`langchain-kotlin`" + `, ` + "`notebox`" + `, ` + "`my-app`" + `).
КАЖДАЯ команда playwright-cli обязана использовать ` + "`-s=<session>`" + `. Без ` + "`-s`" + ` — команды идут в общую сессию и мешают другим окнам.

**ЗАПРЕЩЕНО выводить окно браузера на передний план** — никакого osascript, wmctrl, xdotool, focus-команд и прочего. Пользователь сам переключится когда нужно.

**ЗАПРЕЩЕНО открывать новый браузер без прохождения обязательных проверок** — строгий порядок:

**ШАГ 1 — ОБЯЗАТЕЛЕН:** ` + "`playwright-cli list`" + ` — получить список всех сессий и статус (` + "`open`" + ` / ` + "`closed`" + `)

**ШАГ 2 — нужная сессия ` + "`open`" + `:** использовать напрямую (` + "`goto`" + `, ` + "`snapshot`" + ` и т.д.). Шаг ` + "`open`" + ` ПРОПУСКАЕТСЯ.

**ШАГ 3 — нужная сессия ` + "`closed`" + `:** НЕ открывать браузер сразу. Перебрать ВСЕ ` + "`open`" + ` сессии:
- Сделать ` + "`snapshot`" + ` каждой открытой сессии
- Проверить URL страницы — возможно нужный сайт (тот же домен/хост) уже открыт там
- Если сайт найден в другой сессии — **использовать её**, сессию с нужным именем НЕ открывать
- Пользователь уже залогинен там — его сессия приоритетнее новой

**ШАГ 4 — только если ни одна ` + "`open`" + ` сессия не имеет нужного сайта:** спросить пользователя через ` + "`AskUserQuestion`" + `:
- "Сессия ` + "`<name>`" + ` закрыта, а нужный сайт не найден в открытых браузерах. Открыть новый браузер или переключиться на существующую вкладку вручную?"
- **НЕ открывать браузер автоматически без подтверждения пользователя**

**Запрещено:**
- Открывать сессию с новым профилем без явного разрешения пользователя
- Пытаться запустить ` + "`playwright-cli open`" + ` только потому что нужная именованная сессия закрыта
- Игнорировать уже открытые браузеры с залогиненными аккаунтами
3. ` + "`playwright-cli -s=<session> snapshot`" + ` — получить снимок DOM с ref-ами элементов (e1, e2, ...)
4. ` + "`playwright-cli -s=<session> click <ref>`" + ` / ` + "`playwright-cli -s=<session> fill <ref> <text>`" + ` — взаимодействие
5. ` + "`playwright-cli -s=<session> screenshot`" + ` — скриншот для отчёта
6. Сессия сохраняется между командами — состояние (cookies, localStorage) персистентно
7. Профиль на сессию: ` + "`~/.playwright-profile/<session>`" + ` — логин нужен один раз, затем кэшируется навсегда
8. Если ` + "`click <ref>`" + ` выдаёт "Ref not found" — сделать ` + "`snapshot`" + ` заново и взять актуальный ref

**Шаг 6:** После прохождения каждого шага — обновить файл сценария, пометив шаг как выполненный:

` + "```markdown" + `
- [x] 1. Открыть экран X ✅ (проверено)
- [ ] 2. Нажать кнопку Y
` + "```" + `

**ВАЖНО — устойчивость к компактизации контекста:**
- Файл ` + "`*-e2e-scenario.md`" + ` является персистентным состоянием валидации
- Перед каждым действием в Validation — ПЕРЕЧИТАТЬ файл сценария через Read tool
- Выполненные шаги (` + "`[x]`" + `) — НЕ проверять повторно
- Продолжать с первого невыполненного шага (` + "`[ ]`" + `)
- Это гарантирует что после компактизации тесты не начнутся заново и не будут плавать

**Шаг 7:** Если есть ошибки — откат с описанием проблем. Файл сценария сохраняется, невыполненные шаги остаются.

### Report — сохранение отчётов

Отчёт каждой задачи сохраняется в файл в папку ` + "`./swarm-report/`" + `. Формат:

` + "```" + `
./swarm-report/<slug>-<YYYY-MM-DD>.md
` + "```" + `

Пример: ` + "`./swarm-report/telegram-notifications-2026-02-20.md`" + `

---

## Дизайн-система проекта

### Конвенция

Каждый проект может указать дизайн-систему в своём ` + "`CLAUDE.md`" + ` одной строкой:

` + "```" + `
DESIGN_SYSTEM: <name>
` + "```" + `

Пример: ` + "`DESIGN_SYSTEM: linear`" + `

Файлы дизайн-систем хранятся в ` + "`~/.claude/design-systems/<name>.md`" + `.

### Правило для роли ` + "`ui`" + ` (консилиум)

Агент роли ` + "`ui`" + ` **ОБЯЗАН** в конце своего анализа:

1. Прочитать ` + "`CLAUDE.md`" + ` проекта и найти строку ` + "`DESIGN_SYSTEM:`" + `
2. **Если найдена** — прочитать ` + "`~/.claude/design-systems/<name>.md`" + ` и использовать как руководство по дизайну в своём анализе (цвета, типографика, компоненты)
3. **Если НЕ найдена** — добавить в конец своего ответа блок предупреждения
`
